import Logo from "@/app/components/Logo";

export default function DefaultPage() {
    return (
        <span className="h-full flex justify-center">
            <Logo className="w-32 text-muted-foreground" />
        </span>
    );
}